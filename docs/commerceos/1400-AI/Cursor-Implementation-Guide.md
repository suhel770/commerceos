# Cursor Guide

Implement AI as an optional service layer. Never hard-code prompts. Keep providers replaceable, log AI actions, require approval for destructive actions and degrade gracefully when AI is unavailable.