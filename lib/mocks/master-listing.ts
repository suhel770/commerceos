/* ============================================================================
   CommerceOS
   Mock Master Listings
   ----------------------------------------------------------------------------
   Temporary in-memory data until Repository/API implementation is connected.
   Demo catalog cleared — create listings via Product Studio.
   ============================================================================ */

import type { MasterListing } from "@/lib/types/master-listing";

export const masterListings: MasterListing[] = [];

export default masterListings;
